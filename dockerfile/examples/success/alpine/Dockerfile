# Simple Alpine image that can run shell commands
FROM alpine:3.19

# Demonstrate a build step
RUN echo "hello from alpine"

# Default command prints a ready message
CMD ["sh", "-lc", "echo Alpine container ready"]
