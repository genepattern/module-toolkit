# Minimal Python-based image
FROM python:3.11-slim

# Simple build-time check
RUN python -c "print('hello from python image')"

# Default command
CMD ["python", "-c", "print('Python container ready')"]
