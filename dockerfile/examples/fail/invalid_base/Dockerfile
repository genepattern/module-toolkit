# This Dockerfile should fail because the base image does not exist
FROM this-image-does-not-exist-1234567890:latest

# This will never run, but keeps the format plausible
RUN echo "should not reach here"
